# One Mission One Month のBluesky更新ログ取得に使う認証情報。
# Bluesky設定 → プライバシーとセキュリティ → App Passwords で発行したものを使う（アカウント本体のパスワードは使わない）。
# .env にコピーして値を埋める（.env はgit管理外）。
BLUESKY_HANDLE=your-handle.bsky.social
BLUESKY_APP_PASSWORD=xxxx-xxxx-xxxx-xxxx
